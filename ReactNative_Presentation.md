<div dir="rtl">

# מדריך מפורט להצגת React Native (עם דוגמה מהפרויקט)

מסמך זה נועד לשמש כבסיס מלא למצגת על React Native, עם דוגמאות ישירות מהפרויקט **Todo-ReactNative**.  
הוא כולל הסבר תיאורטי, דוגמאות מהקוד, רעיונות לשקפים, והקשר מעשי.

---

## 1) מה זה React Native?

**React Native** הוא פריימוורק לפיתוח אפליקציות מובייל (Android ו‑iOS) בעזרת **JavaScript/TypeScript** ו‑**React**.  
במקום לכתוב קוד נפרד לכל מערכת, כותבים קוד אחד שמרנדר רכיבים **נאטיביים**.

**העיקרון המרכזי:**
- כותבים קוד אחד → מקבלים אפליקציות לשתי הפלטפורמות.

**מה ההבדל מריאקט רגיל (Web)?**
- React Web מרנדר HTML.
- React Native מרנדר רכיבי UI נאטיביים (כמו View, Text, ScrollView).
- אין DOM, אין CSS רגיל — יש StyleSheet.

---

## 2) למה React Native חשוב?

**יתרונות עיקריים:**
- זמן פיתוח קצר יותר (קוד אחד).
- צוות אחד במקום שניים (iOS + Android).
- שימוש בידע קיים מריאקט.
- ביצועים קרובים לנאטיב.
- קהילה גדולה ואקוסיסטם עשיר.

**מגבלות נפוצות:**
- לפעמים צריך כתיבה של קוד נאטיבי (Java/Kotlin/Swift).
- תלות בספריות צד שלישי.
- ביצועים מורכבים במסכים כבדים במיוחד.

---

## 3) מבנה הפרויקט (ממש בקוד שלך)

בפרויקט הזה יש To‑Do app פשוט. זה מאפשר להראות את כל עקרונות הליבה.

**קבצים מרכזיים:**

- `App.tsx`  
  נקודת הכניסה של האפליקציה. פשוט מחזירה את ה‑Navigator.

- `AppNavigator.tsx`  
  מגדיר את מסכי האפליקציה ואת הניווט ביניהם.

- `screens/TaskListScreen.tsx`  
  מציג את רשימת המשימות, כולל מחיקה ועריכה.

- `screens/AddTaskScreen.tsx`  
  מאפשר להוסיף משימה חדשה.

- `types.ts`  
  הגדרות TypeScript משותפות (למשל Task והניווט).

---

## 4) רכיבי UI בסיסיים בריאקט נייטיב

React Native משתמשת ברכיבים מובנים שמחליפים HTML.

דוגמאות מתוך הפרויקט:
- `View` — контейнер/עוטף של רכיבים.
- `Text` — טקסט למסך.
- `TextInput` — שדה קלט.
- `FlatList` — הצגת רשימות בצורה יעילה.
- `TouchableOpacity` — רכיב לחיצה (כמו כפתור מותאם).

**חשוב להבין:**
אין `<div>`, `<span>` או `<button>`.  
הכול נעשה עם רכיבי React Native.

---

## 5) עיצוב (Styling) בריאקט נייטיב

אין CSS רגיל — יש StyleSheet.

בפרויקט:
- כל מסך מגדיר `const styles = StyleSheet.create({ ... })`.
- הסגנונות הם אובייקטים ב‑JS.

**לדוגמה:**
- `backgroundColor` במקום `background-color`.
- `fontSize`, `marginBottom`, `borderRadius` וכו'.

**יתרון:**  
אותו קוד עובד לשתי הפלטפורמות.

---

## 6) State ו‑Hooks (ניהול מצב)

ב‑React Native משתמשים בדיוק באותו מודל כמו React:

### useState
למשל ב־`AddTaskScreen.tsx`:
- `taskName`, `taskDescription`, `taskCategory` נשמרים בזיכרון המסך.
כל שינוי בקלט משנה את ה‑state.

### useEffect
ב־`TaskListScreen.tsx`:
- `useEffect` טוען משימות מה־AsyncStorage ברגע שהמסך נטען.
- `useEffect` נוסף שומר משימות כאשר state משתנה.

כך נוצר חיבור בין UI לבין הנתונים.

---

## 7) ניווט בין מסכים (React Navigation)

ב־React Native אין דפדפן, ולכן משתמשים בספריות ניווט.

בפרויקט:
```tsx
<Stack.Navigator initialRouteName="TaskList">
  <Stack.Screen name="TaskList" component={TaskListScreen} />
  <Stack.Screen name="AddTask" component={AddTaskScreen} />
</Stack.Navigator>
```

**מה קורה בפועל?**
- TaskList הוא המסך הראשי.
- AddTask נפתח כאשר משתמש לוחץ על כפתור הוספה.

המעבר נעשה ע"י:
```tsx
navigation.navigate('AddTask', { tasks, setTasks });
```

---

## 8) TypeScript בפרויקט

בפרויקט הזה אנחנו משתמשים ב‑TypeScript כדי למנוע טעויות.

### דוגמה: סוג הנתונים של Task
ב־`types.ts`:
```ts
export type Task = {
  id: string;
  name: string;
  description: string;
  category: string;
};
```

כך כל משימה חייבת להכיל את 4 השדות האלה.

### ניווט בטוח
`RootStackParamList` מגדיר אילו מסכים קיימים ואילו פרמטרים מותר להעביר.

**יתרון:**  
אם מנסים לעבור למסך שלא קיים או להעביר פרמטר חסר — TypeScript יצביע על שגיאה.

---

## 9) אחסון מקומי (AsyncStorage)

במובייל צריך לשמור נתונים גם אחרי שהאפליקציה נסגרת.

בפרויקט:
- שמירה נעשית עם `AsyncStorage.setItem('tasks', ...)`.
- טעינה נעשית עם `AsyncStorage.getItem('tasks')`.

זה מאפשר:
✅ רשימת משימות שנשמרת  
✅ נתונים שלא נעלמים אחרי ריסטארט

---

## 10) זרימת משתמש (User Flow) לדוגמה

1. האפליקציה נפתחת → TaskList נטען.
2. המשתמש לוחץ על כפתור +.
3. המסך AddTask נפתח.
4. המשתמש מזין נתונים ולוחץ "Add Task".
5. המשימה נשמרת ונכנסת לרשימה.
6. המסך חוזר לרשימה עם עדכון מיידי.

זה מדגים:
ניווט → טפסים → State → Storage.

---

## 11) React Native לעומת Native

| מאפיין | Native | React Native |
|--------|--------|--------------|
| שפה | Swift / Kotlin | JS / TS |
| קוד כפול | כן | לא |
| גישה לחומרה | מלאה | דרך ספריות |
| זמן פיתוח | ארוך יותר | קצר יותר |

React Native נותן איזון בין נוחות לפיתוח לבין ביצועים טובים.

---

## 12) רעיונות לשקפים במצגת

אפשר לבנות מצגת בסדר כזה:

1. כותרת: "React Native – פיתוח מובייל עם JavaScript"
2. מה זה React Native?
3. יתרונות וחסרונות
4. מבנה הפרויקט (עם תרשים קבצים)
5. רכיבי UI בסיסיים
6. State ו‑Hooks
7. ניווט בין מסכים
8. אחסון מקומי
9. TypeScript ולמה זה עוזר
10. דמו: מסך רשימה + מסך הוספה
11. סיכום

---

## 13) סיכום קצר (לשקף אחרון)

**React Native מאפשר:**
- לבנות אפליקציות Android + iOS בקוד אחד.
- להשתמש בידע של React.
- לפתח מהר יותר עם UI נאטיבי.

**הפרויקט כאן מדגים:**
ניווט, State, עיצוב, ואחסון מקומי — כל הבסיס לאפליקציית מובייל אמיתית.

---

## 14) הרחבות אפשריות (לשאלות בסוף)

אם רוצים להמשיך:
- חיבור ל‑API חיצוני.
- התחברות משתמשים.
- התראות (Push).
- שימוש במצלמה / GPS.
- הפצה דרך EAS / App Store.

---

סוף.

</div>
